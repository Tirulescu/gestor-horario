/** Placeholder ligero mientras carga el chunk del client page (sin skeleton gris). */
export default function PageStackBoot() {
  return <div className="page-stack client-page-boot" aria-hidden />;
}
